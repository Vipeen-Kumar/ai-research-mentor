# Alembic Migrations

Use this directory for schema migrations once the first persistent models are added.

## Common Commands

```bash
alembic revision --autogenerate -m "create initial tables"
alembic upgrade head
```
